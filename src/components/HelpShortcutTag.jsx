import React from 'react'

const HelpShortcutTag = () => {
  return (
    <React.Fragment>
        <div className="hidden lg:block fixed bottom-5 right-5 bg-terminal text-black text-xs font-semibold font-mono px-3 py-2 rounded-xl">Press ? for Help</div>
    </React.Fragment>
  )
}

export default HelpShortcutTag
